package OCP.coffee_machine_service.bad;

public class CoffeeMachine {

    void prepareBeverage(String beverageName){
        if("CAPPUCCINO".equals(beverageName)) {
            //prepare cappuccino
        } else if("ESPRESSO".equals(beverageName)) {
            //prepare espresso
        }
    }
}
